<nav class="site-nav" id="nav" aria-label="Primary navigation">
  <div class="wrap nav-inner">
    <a class="logo" href="<%- page.layout === 'index' ? '#top' : url_for('/#top') %>" aria-label="Lily Liu, home">Lily<span>.</span></a>

    <div class="nav-links" id="navLinks">
      <a href="<%- page.layout === 'index' ? '#work' : url_for('/#work') %>">Work</a>
      <a href="<%- page.layout === 'index' ? '#experience' : url_for('/#experience') %>">Experience</a>
      <a href="<%- page.layout === 'index' ? '#about' : url_for('/#about') %>">About</a>
      <a<% if (page.layout === 'blog' || page.layout === 'post') { %> class="active" aria-current="page"<% } %> href="<%- url_for('/blog/') %>">Blog</a>
      <a class="button button-small" href="<%- page.layout === 'index' ? '#contact' : url_for('/#contact') %>">Let’s talk</a>
    </div>

    <button class="nav-toggle" id="navBtn" type="button" aria-label="Open menu" aria-controls="navLinks" aria-expanded="false">
      <span></span><span></span><span></span>
    </button>
  </div>
</nav>
